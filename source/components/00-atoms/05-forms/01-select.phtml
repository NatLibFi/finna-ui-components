<div class="form-group">
  <label for="<?= $inputName; ?>-select"><?= $label; ?></label>
  <select id="<?= $inputName; ?>-select" name="<?= $inputName; ?>-select" class="form-control">
    <?php foreach ($inputSelectValues as $option) : ?>
      <option><?= $option; ?></option>
    <?php endforeach; ?>
  </select>
</div>
